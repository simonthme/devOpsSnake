# devOpsSnake

On host machine :
install virtual box
install vagrant 
intall ansible

from github:
clone repository

go to SuperSnake/Vagrant

in terminal do:
vagrant up --provision

on host machine browser:

- http://0.0.0.0:5000

SEE THE RESULT ! 
